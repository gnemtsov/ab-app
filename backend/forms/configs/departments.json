{
    "title": {
		"label": "Title",
		"placeholder": "Department title",
		"type": "String",
		"validators": {
			"required": ["Field is required"],
			"length": ["Title must be bigger than 6 and smaller than 64 chars", 6, 64]
		},
		"backend": {
			"db_table": "departments",
			"db_column": "d_title"
		}
    },
    "type": {
		"label": "Department type",
		"description": "Select type from available options",
		"value": "general",
		"type": "String",
		"allowedValues": ["administrative", "financial", "general"]
    },
    "secret": {
		"label": "Secret",
		"description": "A secret word, every department worker must know",
		"noEcho": true,
		"type": "String"
    },
    "magic": {
		"label": "Type of magic",
		"description": "Type of magic the department works on",
		"value": "white",
		"validators": {
			"activeWhite": ["Sorry, only white magic is allowed"]
		},
		"type": "String",
		"allowedValues": ["black", "white"]
    },
    "active": {
		"label": "Active",
		"value": true,
		"description": "Whether department is working or not",
		"validators": {
			"active": ["You are not allowed to create inactive departments"]
		},
		"type": "Boolean"
    },
    "computersUsed": {
		"label": "Number of computers",
		"type": "Number",
		"validators": {
			"typeNum": ["Must be an integer number"],
			"countMin": ["There must be at least 5 computers", 5]
		}
    },
    "description": {
		"label": "Description",
		"type": "Text"
    }
}
